import { HereProviderRequest, HereProviderResult } from "./provider";
import { HereStrategy } from "./types";
export declare const defaultUrl = "https://my.herewallet.app/connector/index.html";
export declare class WidgetStrategy implements HereStrategy {
    private static connector?;
    private static isLoaded;
    private messageHandler?;
    readonly options: {
        lazy: boolean;
        widget: string;
    };
    constructor(options?: string | {
        lazy?: boolean;
        widget?: string;
    });
    initIframe(): HTMLIFrameElement;
    onRequested(id: string, request: HereProviderRequest, reject: (p?: string) => void): void;
    postMessage(data: object): void;
    onApproving(): void;
    onSuccess(request: HereProviderResult): void;
    onFailed(request: HereProviderResult): void;
    close(): void;
}
